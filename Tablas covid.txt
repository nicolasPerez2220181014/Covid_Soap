CREATE TABLE COMPRA_VACUNA (NUMERO_FACTURA number(10) NOT NULL, CANTIDAD_COMPRADAS number(10) NOT NULL, ESTADO_COMPRA varchar2(40) NOT NULL, VALOR_TOTAL number(10) NOT NULL, PRIMARY KEY (NUMERO_FACTURA));
CREATE TABLE DETALLE_COMPRA_VACUNA (SERIAL_COMPRA number(10) NOT NULL, VALOR_COMPRADAS number(10) NOT NULL, CANTIDAD_COMPRA number(10) NOT NULL, DES_COMPRA varchar2(255) NOT NULL, UBICACION_COMPRA varchar2(255) NOT NULL, UBICACION_DESTINO varchar2(100) NOT NULL, COMPRA_VACUNANUMERO_FACTURA number(10) NOT NULL, PROVEEDOR_VACUNAID_PROVEEDOR number(10) NOT NULL, PRIMARY KEY (SERIAL_COMPRA, COMPRA_VACUNANUMERO_FACTURA, PROVEEDOR_VACUNAID_PROVEEDOR));
CREATE TABLE PROVEEDOR_VACUNA (ID_PROVEEDOR number(10) NOT NULL, NOMBRE varchar2(150) NOT NULL, STOCK_VACUNAS number(10) NOT NULL, PAIS_PROVEEDOR varchar2(170) NOT NULL, PRIMARY KEY (ID_PROVEEDOR));
ALTER TABLE DETALLE_COMPRA_VACUNA ADD CONSTRAINT FKDETALLE_CO154281 FOREIGN KEY (COMPRA_VACUNANUMERO_FACTURA) REFERENCES COMPRA_VACUNA (NUMERO_FACTURA);
ALTER TABLE DETALLE_COMPRA_VACUNA ADD CONSTRAINT FKDETALLE_CO412778 FOREIGN KEY (PROVEEDOR_VACUNAID_PROVEEDOR) REFERENCES PROVEEDOR_VACUNA (ID_PROVEEDOR);
CREATE SEQUENCE seq_COMPRA_VACUNA;
CREATE SEQUENCE seq_PROVEEDOR_VACUNA;
